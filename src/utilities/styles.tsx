import { StyleSheet } from "react-native";

export const globalStyles = StyleSheet.create({
  regularText: {
    fontSize: 16,
  },
  title: {
    fontSize: 20,
    paddingBottom: 8,
  },
  subtitle: {
    fontSize: 18,
    paddingBottom: 8,
  },
});
